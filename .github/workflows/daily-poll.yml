name: Daily Competitive Poll

on:
  schedule:
    # Business days only, at 15:00 UTC. GitHub cron ignores DST, so this
    # lands at 08:00 Pacific during PDT (~Mar–Nov) and 07:00 during PST —
    # biased early (like the newsletter cron) so it's a morning alert
    # year-round and absorbs GitHub's usual 10–60 min cron delay.
    # 1-5 = Mon–Fri; 15:00 UTC is the same weekday in Pacific. No weekend
    # alerts (Saturday/Sunday mornings are skipped).
    # 08:00 runs BEFORE the day's cd.io crawl (~09:00–15:00 Pacific), so each
    # morning's alert covers the PRIOR day's detections. The job uses a 76h
    # lookback to bridge the weekend gap: Friday's crawl is alerted Monday
    # 08:00. Re-fetched changes already in Notion are skipped by dedup.
    - cron: "0 15 * * 1-5"
  workflow_dispatch: # allow manual trigger from GitHub Actions UI

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: competitive_intel/requirements.txt

      - name: Install dependencies
        run: pip install -r competitive_intel/requirements.txt

      - name: Run daily poll
        working-directory: competitive_intel
                env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          CHANGEDETECTION_API_KEY: ${{ secrets.CHANGEDETECTION_API_KEY }}
          CHANGEDETECTION_BASE_URL: ${{ secrets.CHANGEDETECTION_BASE_URL }}
          NOTION_PARENT_PAGE_ID: ${{ secrets.NOTION_PARENT_PAGE_ID }}
          NOTION_CHANGES_DB_ID: ${{ secrets.NOTION_CHANGES_DB_ID }}
          # Single Teams destination. The 11 per-competitor
          # TEAMS_WEBHOOK_<COMPETITOR> secrets were removed on 2026-08-18:
          # every one of them was null, so alerts already fell through to
          # the general webhook while the config implied 11 live channels.
          TEAMS_GENERAL_WEBHOOK: ${{ secrets.TEAMS_GENERAL_WEBHOOK }}
        run: python -m jobs.daily_poll
